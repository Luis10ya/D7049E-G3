import Mediator from "./Mediator";

/**
 * @class InventoryMediator
 * @extends {Mediator}
 * 
 */

export default class InventoryMediator extends Mediator {
    static instance = new InventoryMediator();

}